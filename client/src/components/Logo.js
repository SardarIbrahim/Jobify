import React from 'react';

import logo from '../assets/images/logo.svg';

const Logo = () => {
  return (
    <div>
      {' '}
      <img src={logo} alt='Logo here' />
    </div>
  );
};

export default Logo;
